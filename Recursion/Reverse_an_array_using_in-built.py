import array as arr

# Let's assume you have an array
array = arr.array('i', [1, 2, 3, 4, 5])

# To reverse the array in Python, you can use the reverse() function
array.reverse()

print(array)  # Output: array('i', [5, 4, 3, 2, 1])
